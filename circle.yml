machine:
  node:
    version: 5.6.0

dependencies:
  override:
    - npm install
    - npm run build

test:
  override:
    - npm test

deployment:
  production:
    branch: [master, dev]
    commands:
      - npm run deploy
