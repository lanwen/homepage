machine:
  node:
    version: 5.6.0

dependencies:
  override:
    - npm install
    - npm run build

test:
  override:
    - echo 'No tests yet'

deployment:
  surge:
    branch: [master, dev]
    commands:
      - npm run deploy
